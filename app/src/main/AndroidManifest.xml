<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".futuroRadio"></activity>
        <activity android:name=".acceso_datos" />
        <activity android:name=".evolucion_radio" />
        <activity android:name=".iinicios_de_la_radio" />
        <activity android:name=".histotria_radio" />
        <activity android:name=".Seccion_Comentarios" />
        <activity android:name=".radioplayer" />
        <activity android:name=".panelRadio" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>